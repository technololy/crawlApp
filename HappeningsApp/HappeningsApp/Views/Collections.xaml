<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"

           NavigationPage.HasNavigationBar="False"
     xmlns:bdEntry="clr-namespace:HappeningsApp.Custom"
             BackgroundColor="Black"
         xmlns:b="clr-namespace:HappeningsApp.Custom"    
             x:Class="HappeningsApp.Views.Collections" >
    <ContentPage.Content>
        <ListView ItemsSource="{Binding collections}" SeparatorVisibility="None" 
                  HasUnevenRows="true" Footer="" RowHeight="150" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                 <!--    <Frame Padding="0" BackgroundColor="Black" VerticalOptions="CenterAndExpand" 
               HeightRequest="150" HasShadow="false" CornerRadius="0" Margin="10" >
                            <Grid ColumnSpacing="0" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <BoxView BackgroundColor="Silver" Grid.Column="0"  WidthRequest="120"/>
                <Frame BackgroundColor="Gray" HasShadow="false" Padding="0" Grid.Column="1" CornerRadius="0" >
                     <StackLayout >
                                    <Label Text="{Binding Name}" VerticalOptions="CenterAndExpand" TextColor="White" />
                                    
                                </StackLayout>
                </Frame>
                               
                            </Grid> 
                        </Frame>-->
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

      
    </ContentPage.Content>
</ContentPage>